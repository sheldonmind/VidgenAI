# Backend API base URL (no trailing slash).
# - Local: run BE and use default below.
# - Production (e.g. Vercel): set this in Vercel Environment Variables
#   so the built app calls your deployed backend (e.g. Railway, Render).
VITE_API_BASE_URL=http://localhost:4000/api/v1
